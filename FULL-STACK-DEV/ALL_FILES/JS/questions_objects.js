// Q1. Create an object called person with properties (name, age and city). Print the object 
// Q2. WAP to print keys of last created object. 
let person = {
    name : "nitin",
    age : 24,
    city : "delhi",
}
console.log(Object.keys(person));

// Q3. Create an object car with properties brand and model. Access print the brand.
// Q4. write a program that prints all values of an objects using console.log().
const car = {
    brand:"Land_Rover",
    model:"Range_Rover",
}
console.log(car);
console.log(Object.values(car));